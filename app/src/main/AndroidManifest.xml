<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nd.centertableinc.popularmovies1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.movie_overview.MovieOverviewActivity"
            android:configChanges="orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.movie_details.MovieDetailsActivity" />

        <provider
            android:name=".data.movies_provider.SQLiteProvider"
            android:authorities="nd.centertableinc.popularmovies1.data.movies_provider"
            android:multiprocess="true"></provider>

        <activity android:name=".activity.movie_details.MovieReviewsActivity"></activity>
    </application>

</manifest>